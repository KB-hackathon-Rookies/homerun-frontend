/**
 * 홈 진행 표시(시안 `SubStep`).
 *
 * 정착 필수 요소 네 가지. 시안은 이 넷을 다 끝내면 사후 관리 모드로 넘어간다
 * (`홈 8 · 대시보드 · 사후 관리 모드`). 사후 관리 화면들은 `current = STEPS.length`
 * 로 전 칸이 체크된 상태를 보여준다.
 */
export const HOME_STEPS = ['반환보증', '보증료 지원', '사후자산심사', '중개보수·이사비'];
